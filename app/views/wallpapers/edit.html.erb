<h1>Editing Wallpaper</h1>
 
<%= form_for :post, url: post_path(@wallpaper.id) },
method: :patch do |f| %>
  <% if @wallpaper.errors.any? %>
  <div id="errorExplanation">
    <h2><%= pluralize(@wallpaper.errors.count, "error") %> prohibited
      this post from being saved:</h2>
    <ul>
    <% @post.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>

  <% end %>
  <p>
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </p>
 
  <p>
    <%= f.submit %>
  </p>
<% end %>
 
<%= link_to 'Back', wallpapers_path %>